<resources>

    <!-- Base application theme. -->
    <style name="PhotoPickerTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="GalleryBottomBarAnim">
        <item name="android:windowEnterAnimation">@anim/bottom_bar_push_out</item>
        <item name="android:windowExitAnimation">@anim/bottom_bar_push_in</item>
    </style>

    <style name="ToolBarTheme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <item name="actionMenuTextColor">#fff</item>
        <item name="android:textSize">16sp</item>
    </style>
</resources>
